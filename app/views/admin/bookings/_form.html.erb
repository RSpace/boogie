<%= form_for([:admin, @booking]) do |f| %>
  <% if @booking.errors.any? %>
    <div id="error_explanation" class="info-box bg-danger text-danger">
      <h4><%= @booking.errors.count %> fejl forhindrede denne booking i at blive gemt:</h4>

      <ul>
      <% @booking.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :booking_date, 'Booking dato' %><br>
    <%= f.date_select :booking_date, {}, :class => 'input-lg' %>
  </div>

  <div class="form-group">
    <%= f.label :user_id, 'Bruger' %><br>
    <%= f.select :user_id, @all_users.collect { |user| [user.username, user.id] }, {}, :class => 'input-lg' %>
    <div class="help-text">Vælg din egen bruger hvis du booker på vegne af en forening til et fællesarrangement. Vælg kun en anden bruger hvis du booker på vegne af en beboer, og I sørger for betalingen uden om dette system.</div>
  </div>

  <div class="form-group">
    <%= f.label :description, 'Beskrivelse' %><br>
    <%= f.text_field :description, :class => 'form-control' %>
    <div class="help-text">Hvis du booker til en foreningsaktivitet; angiv venligst aktiviteten her</div>
  </div>

  <%= f.submit 'Gem booking', :class => 'btn btn-default' %>
<% end %>
